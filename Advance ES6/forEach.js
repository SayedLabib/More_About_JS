// For each kind of similar to the map function 
// but, like map function for each does not return
// any array or, object rather than it just do the work
// IF I want to do more works in an array we will sometimes use
// forEach function on the array instead

const array = [12, 13, 14, 17, 23];

// const result = array.forEach(num => num *2);
const result = array.forEach(num => console.log(num*2));




